{
  "name": "windows-copilot-api",
  "version": "1.0.0",
  "private": true,
  "description": "Microsoft Copilot bridge with an OpenAI-compatible API",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "login": "python3 -m copilot login",
    "ask": "python3 -m copilot ask",
    "dev": "python3 app.py",
    "start": "python3 app.py",
    "install:playwright": "npx playwright install chromium",
    "setup": "[ -d venv ] || python3 -m venv venv; source venv/bin/activate; pip install -r requirements.txt; npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "1.60.0"
  }
}
